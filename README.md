# Multi-Color Object Detector App

A classical computer vision application to detect objects by color using HSV segmentation, built with Streamlit.

## Setup

```bash
pip install -r requirements.txt
streamlit run app.py
